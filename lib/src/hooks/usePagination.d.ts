export declare const usePagination: (totalItems: number, pageSize: number, pagesToDisplay: number, currentPage: number) => string[];
export declare const isCurrentPage: (page: string, currentPage: number) => boolean;
